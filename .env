# MedLogix API configuration
# In production on Vercel, requests use relative paths (/patients, /logs)
# which vercel.json proxies over HTTPS to the AWS Network Load Balancer.
# Leave blank for automatic proxy/rewrites:
VITE_PATIENT_API=
VITE_AUDIT_API=
